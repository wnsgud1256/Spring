<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">

	<select id="getBoards" resultType="board">
		SELECT * FROM BOARD ORDER BY
		SEQ
	</select>

	<insert id="insertBoardProc" statementType="CALLABLE"
		parameterType="board"> <!-- statementType중요, 프로시저 호출 -->
		{ call BOARD_INS_PROC(
		#{title},
		#{writer},
		#{content,mode=IN,
		jdbcType=VARCHAR, javaType=string},
		#{seq, mode=OUT,jdbcType=NUMERIC,
		javaType=string},
		#{outmsg, mode=OUT, jdbcType=VARCHAR,
		javaType=string} )
		}
	</insert> <!-- javaType은 DTO의 타입이랑 맞아야함 jdbcType=VARCHAR, javaType=string는 둘다 같은 타입 
		이라 생략이 가능함. -->

</mapper>